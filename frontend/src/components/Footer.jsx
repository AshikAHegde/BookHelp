/**
 * Returns the shared footer for the frontend.
 * @returns {JSX.Element}
 */
export function Footer() {
  return (
    <footer className="site-footer">
      <p>BookHelp keeps student workflows simple, fast, and focused.</p>
      <span>Built for login, registration, and a clean home experience.</span>
    </footer>
  )
}
